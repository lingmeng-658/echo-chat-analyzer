# AGENTS.md

# Local Chat Analyzer AI Development Guide

## Project定位

这是一个隐私优先、本地运行的聊天记录分析工具。

目标： - 支持多个聊天来源； - 将不同来源转换为统一消息模型； -
提供高质量聊天分析； - 最终让普通用户无需命令行即可使用。

当前来源： - QQChatExporter - 微信聊天记录 - 其他可扩展来源

------------------------------------------------------------------------

## 隐私规则

禁止： - 上传真实聊天记录； - 提交真实 JSON/JSONL 数据； -
提交群号、昵称、账号信息； - 提交密码、token、API Key。

测试必须使用： - 虚构数据； - 人工构造样本。

真实聊天数据只能在用户本机处理。

------------------------------------------------------------------------

## 架构原则

核心原则：

数据来源与分析逻辑分离。

架构事实来源：

ARCHITECTURE.md（仓库根目录）是当前唯一架构事实来源；架构与分层讨论以它为准，不在本文件复制架构图。

结构：

数据来源 → Parser / Provider → ChatMessage → Application Service →
Analysis Core → Exporter / GUI

分析核心不能知道： - QQ 格式； - 微信格式； - CLI； - GUI； -
第三方工具实现。

新增来源必须先转换为统一消息模型。

------------------------------------------------------------------------

## QQ 数据入口

当前标准链路（QQChatExporter 集成已通过真实环境验收）：

QQChatExporter Provider
        ↓
QCE JSON
        ↓
Adapter
        ↓
ChatMessage
        ↓
ImportService
        ↓
AnalysisApplicationService

架构约束：

- CLI 不直接调用 Provider；
- Provider 只负责外部数据获取；
- Adapter 只负责格式转换；
- Application 层负责业务编排；
- CLI 不直接调用核心分析逻辑；
- 不复制分析逻辑。

------------------------------------------------------------------------

## 模块职责

### Parser

负责： - 读取外部数据； - 转换内部模型。

不负责： - 分析； - 过滤策略。

### Cleaner

负责： - 文本清理； - 标准化。

不负责： - 判断机器人； - 判断消息价值。

### Tokenizer

负责： - 分词； - token生成。

### Analyzer

负责： - 统计； - 用户分析； - 关系分析。

不能包含： - 文件读取； - CLI逻辑； - GUI逻辑。

### Application Service

CLI 和 GUI 的共同入口。

GUI 通过 ChatAnalyzerFacade 接入（见 GUI原则）。

禁止出现两套业务逻辑。

------------------------------------------------------------------------

## 智能过滤原则

过滤分三个层级：

### 发送者过滤

过滤整个来源。

例如： - 机器人账号； - 系统账号。

### 消息模板过滤

过滤完整重复模板。

例如： - 欢迎消息； - 签到回复； - 查询结果。

不要把模板中的词全部加入停用词。

### 停用词过滤

只影响分词结果。

自动识别需要记录： - 理由； - 类别； - 置信度。

支持人工覆盖和撤销。

------------------------------------------------------------------------

## AI开发流程

修改代码前：

1.  阅读相关代码；
2.  理解当前设计；
3.  说明方案；
4.  确认影响范围。

不要： - 大规模重构； - 为未确定需求提前设计复杂框架； - 修改无关文件。

新功能：

设计 → RED测试 → 实现 → GREEN测试 → 完整测试 → 检查diff

完成前运行：

pytest

git diff --check

------------------------------------------------------------------------

## 第三方工具集成

第三方工具只是数据来源。

例如： - CipherTalk CLI； - QQChatExporter。

推荐：

第三方工具 → Provider / Adapter → ChatMessage → 分析系统

不要： - 将第三方代码复制进核心； - 让核心依赖具体工具。

------------------------------------------------------------------------

## GUI原则

GUI只负责展示和交互。

不能包含： - 数据解析； - 分析算法； - 过滤规则。

正确：

GUI → ChatAnalyzerFacade → Application Service → Core

约束：

- GUI 必须通过 ChatAnalyzerFacade 调用业务能力，不得绕过 Facade；
- GUI 不得直接调用 Provider / Parser / Adapter / Analysis Core；
- 禁止跨层依赖：GUI 不 import 业务层以外的内部模块；
- 错误统一使用 FacadeError.public_message，不展示 traceback。

------------------------------------------------------------------------

## 产品优先级

优先：

1.  能运行；
2.  用户能使用；
3.  支持更多来源；
4.  提升分析质量；
5.  优化界面。

不要为了完美架构阻碍产品落地。

------------------------------------------------------------------------

## AI Agent定位

AI是开发助手，不是项目决策者。

AI负责： - 编码； - 调查； - 测试； - 提供方案。

项目方向由人工决定。

重大架构变化： 先讨论，再实施。
